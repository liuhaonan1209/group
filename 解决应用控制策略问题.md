# Windows 应用控制策略问题解决方案

## 🔍 问题描述

错误信息: `An Application Control policy has blocked this file`

这是 Windows 应用程序控制策略（WDAC/AppLocker）阻止了 Go 编译生成的可执行文件运行。

## 🛠️ 解决方案

### 方案1: 使用预编译（推荐）

1. **编译所有程序**:
   ```cmd
   # 运行编译脚本
   build_and_run.bat
   ```

2. **启动服务**:
   ```cmd
   # 启动所有服务
   start_services.bat
   ```

3. **手动运行可执行文件**:
   ```cmd
   # 进入 bin 目录
   cd bin
   
   # 运行各个服务
   routeRpc.exe
   financeRpc.exe
   routerApi.exe
   financeApi.exe
   seed_data.exe
   ```

### 方案2: 修改系统策略（需要管理员权限）

#### 2.1 禁用 Windows Defender Application Control

1. **以管理员身份运行 PowerShell**
2. **检查当前策略**:
   ```powershell
   Get-CimInstance -ClassName Win32_DeviceGuard -Namespace root\Microsoft\Windows\DeviceGuard
   ```

3. **临时禁用 WDAC**:
   ```powershell
   # 设置为审核模式（不阻止，只记录）
   Set-HVCIOptions -Enabled $false
   ```

#### 2.2 修改 AppLocker 策略

1. **打开组策略编辑器**:
   ```cmd
   gpedit.msc
   ```

2. **导航到**:
   ```
   计算机配置 > Windows 设置 > 安全设置 > 应用程序控制策略 > AppLocker
   ```

3. **添加例外规则**:
   - 右键 "可执行规则" > 创建新规则
   - 选择 "路径" 规则
   - 添加 Go 临时目录路径: `%LOCALAPPDATA%\go-build\*`

### 方案3: 使用不同的编译方式

#### 3.1 静态编译
```cmd
set CGO_ENABLED=0
go build -a -ldflags '-extldflags "-static"' -o app.exe .
```

#### 3.2 使用 UPX 压缩
```cmd
# 先编译
go build -o app.exe .

# 再压缩（可能绕过某些检测）
upx --best app.exe
```

### 方案4: 使用容器化部署

#### 4.1 Docker 方式
```dockerfile
FROM golang:1.21-alpine AS builder
WORKDIR /app
COPY . .
RUN go build -o main .

FROM alpine:latest
RUN apk --no-cache add ca-certificates
WORKDIR /root/
COPY --from=builder /app/main .
CMD ["./main"]
```

#### 4.2 使用 WSL2
```bash
# 在 WSL2 中运行
wsl
cd /mnt/c/Users/24815/Desktop/group
go run api/routerApi/main.go
```

### 方案5: 企业环境解决方案

#### 5.1 联系 IT 管理员
- 请求将 Go 编译目录添加到白名单
- 申请开发者例外权限
- 请求临时禁用应用控制策略

#### 5.2 使用企业批准的开发工具
- 使用 Visual Studio Code 的 Go 扩展
- 使用 GoLand IDE
- 使用企业批准的编译环境

## 🚀 快速解决步骤

### 立即可用的解决方案:

1. **运行编译脚本**:
   ```cmd
   build_and_run.bat
   ```

2. **检查 bin 目录**:
   ```cmd
   dir bin
   ```

3. **直接运行可执行文件**:
   ```cmd
   bin\routerApi.exe
   ```

### 如果编译也被阻止:

1. **使用 WSL2**:
   ```bash
   wsl
   cd /mnt/c/Users/24815/Desktop/group/api/routerApi
   go run .
   ```

2. **使用在线 IDE**:
   - GitHub Codespaces
   - GitPod
   - Repl.it

## ⚠️ 注意事项

1. **不要禁用安全策略** - 除非你完全理解风险
2. **使用预编译方式** - 这是最安全的解决方案
3. **联系系统管理员** - 在企业环境中获得正确的权限
4. **备份重要数据** - 在修改系统策略前

## 📞 获取帮助

如果以上方案都不能解决问题:

1. 联系你的 IT 部门
2. 查看 Windows 事件日志获取详细错误信息
3. 考虑使用虚拟机进行开发
4. 使用云端开发环境