# 数据库批量插入脚本 - 使用说明

## 🎯 功能概述

为班车管理系统生成大量测试数据，支持每个表插入100万条数据，总计900万条记录。

## 📁 脚本文件

```
scripts/
├── bulk_insert_sql.py      # Python高性能批量插入脚本 (推荐)
├── bulk_insert_data.go     # Go语言版本插入脚本
├── quick_test_data.py      # 快速测试脚本 (每表1万条)
├── run_bulk_insert.sh      # Linux/Mac执行脚本
├── run_bulk_insert.bat     # Windows执行脚本
└── README.md              # 详细说明文档
```

## 🚀 快速开始

### Windows系统

```cmd
# 1. 进入脚本目录
cd scripts

# 2. 安装Python依赖
pip install pymysql

# 3. 执行批量插入 (选择其一)
# 方式1: 直接执行Python脚本
python bulk_insert_sql.py

# 方式2: 使用批处理脚本
run_bulk_insert.bat

# 方式3: 快速测试 (推荐先测试)
python quick_test_data.py
```

### Linux/Mac系统

```bash
# 1. 进入脚本目录
cd scripts

# 2. 安装Python依赖
pip3 install pymysql

# 3. 执行批量插入
# 方式1: 使用执行脚本
./run_bulk_insert.sh

# 方式2: 直接执行Python脚本
python3 bulk_insert_sql.py

# 方式3: 快速测试
python3 quick_test_data.py
```

## ⚙️ 数据库配置

在执行前，请确保修改脚本中的数据库配置：

```python
DB_CONFIG = {
    'host': 'localhost',      # 数据库主机
    'port': 3306,            # 数据库端口
    'user': 'root',          # 数据库用户名
    'password': 'password',   # 数据库密码 (请修改)
    'database': 'bus_management',  # 数据库名
    'charset': 'utf8mb4'
}
```

## 📊 数据规模

### 完整版本 (`bulk_insert_sql.py`)
- **每个表**: 1,000,000 条记录
- **总记录数**: 9,000,000 条
- **预计耗时**: 2-5 分钟
- **磁盘空间**: 约 3-5 GB

### 快速测试版本 (`quick_test_data.py`)
- **每个表**: 10,000 条记录
- **总记录数**: 40,000 条
- **预计耗时**: 10-30 秒
- **磁盘空间**: 约 50 MB

## 🗃️ 数据表清单

| 表名 | 中文名称 | 记录数 | 说明 |
|------|----------|--------|------|
| `start_stations` | 站点表 | 1,000,000 | 公交站点信息 |
| `drivers` | 司机表 | 1,000,000 | 司机基本信息 |
| `passengers` | 乘客表 | 1,000,000 | 乘客基本信息 |
| `routes` | 路线表 | 1,000,000 | 班车路线信息 |
| `bus_schedules` | 班次表 | 1,000,000 | 班车班次安排 |
| `route_stops` | 路线站点关联表 | 1,000,000 | 路线与站点关系 |
| `transactions` | 交易明细表 | 1,000,000 | 交易记录 |
| `balance_sheets` | 收支对账表 | 1,000,000 | 财务对账数据 |
| `driver_settles` | 司机结算表 | 1,000,000 | 司机结算记录 |

## 🔧 性能特性

### Python脚本优势
- ✅ **多线程并发**: 4个线程同时插入
- ✅ **批量操作**: 每批5000条记录
- ✅ **连接池**: 优化数据库连接
- ✅ **错误处理**: 完善的异常处理
- ✅ **进度显示**: 实时显示插入进度
- ✅ **性能统计**: 显示插入速度

### 数据特征
- **真实性**: 使用真实的中国城市名和姓名
- **多样性**: 随机生成各种数据组合
- **关联性**: 表间数据具有合理关联
- **时间分布**: 数据时间跨度1-3年

## 📝 执行步骤

### 1. 环境准备
```bash
# 检查Python版本 (需要3.6+)
python --version

# 检查MySQL服务状态
# Windows: 服务管理器中查看MySQL服务
# Linux: systemctl status mysql
```

### 2. 数据库准备
```sql
-- 创建数据库 (如果不存在)
CREATE DATABASE IF NOT EXISTS bus_management 
CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 创建用户并授权 (可选)
CREATE USER 'bus_user'@'localhost' IDENTIFIED BY 'bus_password';
GRANT ALL PRIVILEGES ON bus_management.* TO 'bus_user'@'localhost';
FLUSH PRIVILEGES;
```

### 3. 执行插入
```bash
# 建议先执行快速测试
python quick_test_data.py

# 确认无误后执行完整插入
python bulk_insert_sql.py
```

### 4. 验证数据
```sql
-- 检查各表记录数
SELECT 
    table_name,
    table_rows
FROM information_schema.tables 
WHERE table_schema = 'bus_management'
ORDER BY table_rows DESC;

-- 查看示例数据
SELECT * FROM start_stations LIMIT 5;
SELECT * FROM drivers LIMIT 5;
SELECT * FROM transactions LIMIT 5;
```

## ⚠️ 注意事项

### 执行前检查
- ✅ MySQL服务已启动
- ✅ 数据库连接配置正确
- ✅ 用户权限足够 (CREATE, INSERT)
- ✅ 磁盘空间充足 (建议10GB+)
- ✅ 内存充足 (建议4GB+)

### 执行过程中
- 🔄 脚本会自动创建表结构
- 🔄 支持中断后重新执行
- 🔄 显示实时进度和性能统计
- 🔄 出现错误会显示详细信息

### 执行后
- 📊 验证数据完整性
- 📊 检查表索引是否正常
- 📊 可根据需要清理测试数据

## 🛠️ 故障排除

### 常见错误及解决方案

1. **连接失败**
   ```
   错误: Can't connect to MySQL server
   解决: 检查MySQL服务、主机地址、端口
   ```

2. **权限不足**
   ```
   错误: Access denied for user
   解决: 检查用户名密码，确保有足够权限
   ```

3. **内存不足**
   ```
   解决: 减少BATCH_SIZE或THREAD_COUNT参数
   ```

4. **磁盘空间不足**
   ```
   解决: 清理磁盘空间或减少TOTAL_RECORDS
   ```

### 性能调优建议

```python
# 高性能配置 (适合高配置服务器)
BATCH_SIZE = 10000
THREAD_COUNT = 8

# 低配置设备
BATCH_SIZE = 1000
THREAD_COUNT = 2
```

## 📈 预期性能

### 硬件配置参考
| 配置 | CPU | 内存 | 磁盘 | 预计速度 | 总耗时 |
|------|-----|------|------|----------|--------|
| 高配 | 8核+ | 16GB+ | SSD | 100K/秒 | 1.5分钟 |
| 中配 | 4核 | 8GB | SSD | 50K/秒 | 3分钟 |
| 低配 | 2核 | 4GB | HDD | 20K/秒 | 7.5分钟 |

## 🔍 数据验证

执行完成后建议进行以下验证：

```sql
-- 1. 检查记录数
SELECT COUNT(*) FROM start_stations;  -- 应该是1,000,000
SELECT COUNT(*) FROM drivers;         -- 应该是1,000,000
-- ... 其他表

-- 2. 检查数据质量
SELECT COUNT(DISTINCT name) FROM start_stations;  -- 检查重复
SELECT COUNT(DISTINCT tel) FROM drivers;          -- 检查唯一性

-- 3. 检查数据分布
SELECT DATE(created_at), COUNT(*) 
FROM transactions 
GROUP BY DATE(created_at) 
ORDER BY DATE(created_at) DESC 
LIMIT 10;

-- 4. 检查关联数据
SELECT r.route_no, COUNT(rs.id) as stop_count
FROM routes r
LEFT JOIN route_stops rs ON r.id = rs.route_id
GROUP BY r.id
LIMIT 10;
```

## 🧹 清理数据

如需清理测试数据：

```sql
-- 清空所有表数据 (保留表结构)
TRUNCATE TABLE start_stations;
TRUNCATE TABLE drivers;
TRUNCATE TABLE passengers;
TRUNCATE TABLE routes;
TRUNCATE TABLE bus_schedules;
TRUNCATE TABLE route_stops;
TRUNCATE TABLE transactions;
TRUNCATE TABLE balance_sheets;
TRUNCATE TABLE driver_settles;

-- 或删除整个数据库
DROP DATABASE bus_management;
```

## 📞 技术支持

如遇到问题，请按以下顺序排查：

1. **检查数据库连接**: 使用MySQL客户端测试连接
2. **检查Python环境**: 确保Python 3.6+和pymysql已安装
3. **检查系统资源**: 确保内存和磁盘空间充足
4. **查看错误日志**: 脚本会显示详细的错误信息
5. **调整参数**: 根据硬件配置调整批次大小和线程数

---

**提示**: 建议先使用 `quick_test_data.py` 进行小规模测试，确认环境配置正确后再执行完整的批量插入。